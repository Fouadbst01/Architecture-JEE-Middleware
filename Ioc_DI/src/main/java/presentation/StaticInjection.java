package presentation;

public class StaticInjection {
    /*public static void main(String[] args) {
        IDao dao=new IDaoDB();
        //Injection de dependence via Constructeur
        IMetier metier = new IMetierImpl(dao);
        //Injection de depandence via setter
        IMetier metier2 = new IMetierImpl();
        ((IMetierImpl)metier2).setDao(dao);
        System.out.println("Test Injecton Static par Constructeur :");
        System.out.println(metier.Calculate());
        System.out.println("Test Injecton Static par Setter :");
        System.out.println(metier2.Calculate());
    }*/
}
